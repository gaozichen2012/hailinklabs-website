# AI Agent 工作规则

本文件及以下两个文档面向 Codex、ChatGPT 等 AI Agent，作为项目上下文入口。

## 执行前必读

执行任何任务前必须先读取：

1. `docs/项目工程手册.md`：长期有效的项目定义、边界、架构、设计与约束。
2. `docs/项目当前状态.md`：动态的阶段、工作方向、问题、验证证据与基线。

开始修改前检查 Git 分支、HEAD 和工作区，识别已有改动。未经授权不得覆盖、回退、暂存或提交其他工作产生的修改。

## 信息与变更边界

- 只将源码、配置、有效文档、Git 历史或用户明确确认支持的信息写成事实；不确定信息标注“待确认”“未验证”或“无当前证据”。
- 禁止在未获用户确认的情况下扩大产品范围、修改核心架构或改变协议语义。
- 功能完成、Bug 修复、构建、自动测试和实机或业务验证结果只更新项目当前状态，不因此修改工程手册。
- 只有产品定位、项目边界、系统架构、模块职责、核心协议或数据模型、长期设计原则或冻结规则发生经过用户确认的变化时，才更新工程手册中的长期定义。
- 文档与代码冲突时，不得静默改写长期规则，也不得自行选择某一版本覆盖其他版本；将冲突及证据记录到项目当前状态的问题与风险中，并向用户报告。
- 区分设计、代码实现、编译、自动测试、实机或业务验证。代码存在不等于业务通过；旧提交、旧产物和历史验证结果不能自动作为当前 HEAD 的验证结果。
- 尊重已有更严格的安全、构建和项目边界约束。修改范围以用户授权为准；不得因文档维护自行整理、删除、移动或重命名其他文档，或创建额外任务管理体系。

## 并行工作

- 项目当前状态按独立“工作方向”组织，不按聊天窗口或会话组织；不创建“窗口一”“窗口二”“任务一”“任务二”等聊天映射。
- 每个并行会话只更新自己涉及的工作方向；更新前必须重新读取最新文件，并核对期间发生的改动。
- 不得覆盖、删除或回退其他并行方向的最新信息；发现冲突时保留有效信息并协调合并。
- 修改影响多个方向时，分别更新对应方向并说明关联关系。
- 已完成且不再影响当前工作的方向可以从活跃方向移除，历史由 Git 保存。

## 完成任务

- 任务完成后必须检查并更新项目当前状态，使其反映实际结果、验证层级、剩余问题和下一步；若检查后无实质状态变化，应说明，无需制造流水账。
- 项目当前状态是快照，不是任务管理系统或开发日志；最近重要变化最多保留五条仍影响当前工作的重大变化。
- 提交前检查最终差异并执行 `git diff --check`；仅暂存任务范围内文件。
- 只报告实际执行且有证据的检查结果，不把未运行的构建或测试写为通过。提交与推送必须遵循用户授权。
