# 项目当前状态

本文件是按独立工作方向组织的动态快照；长期规则见 [项目工程手册](项目工程手册.md)。

## 一、项目总体状态

- 快照日期：2026-09-14。
- 当前阶段：官网静态实现与 GitHub Pages 发布准备；正式域名上线被完整 DNS 备份及新网登录阻塞。
- 用户已冻结新网注册/DNS、腾讯企业邮箱、Astro、GitHub Pages 和零新增网站成本；旧 Cloudflare 方案已取消。
- 七个业务页面、404、favicon、robots、sitemap、SEO/Open Graph、桌面和移动布局已实现。不得将本地验证等同生产上线。

## 二、当前工作方向

### 网站与自动部署

- 已接手并保留先前未提交页面工作；原任务已确认停止写入与旧部署。
- 已移除 Wrangler/Cloudflare 配置与依赖，隐私托管说明改为 GitHub Pages。
- 已修复 `.html` canonical 和跳过导航后的正文焦点问题。无客户端脚本、表单、跟踪或后端。
- 仓库已由 Private 改为 Public；操作前 Gitleaks 8.30.1 扫描两个完整历史提交及 39 个候选文件未发现 secrets。详见 `REPOSITORY_SECRET_AUDIT.md`。
- GitHub API 已启用 Pages Actions source，并绑定 `hailinklabs.com`；实际默认 hostname 是 `gaozichen2012.github.io`。Workflow 完成但尚未推送运行。

### DNS、HTTPS 与邮箱保护

- 两台权威 NS 保持 `ns11.xincache.com`、`ns12.xincache.com`；腾讯 MX 为 priority 5 `mxbiz1.qq.com`、priority 10 `mxbiz2.qq.com`，TTL 600。
- 当前权威查询未见 apex A/AAAA、www CNAME、根域 TXT/SPF 或 DMARC；DKIM selector 未知。这些公开查询不是完整 zone。
- 已保存当前公开快照 `artifacts/dns-pages-before.json`，先前快照 `artifacts/dns-before.json` 保留。**完整 DNS 导出未取得，不允许写入。**
- 无 DNS 写入；Email DNS modified: **NO**。禁止改 Nameserver、MX 或任何邮件记录。
- GitHub custom domain 已保存；证书未签发，Enforce HTTPS 尚不可确认。实际邮件收发与认证头 NOT VERIFIED。

### Apple 官网准备

- 品牌、法律主体、公司域名邮箱和 SameJob 内容已实现；隐私文案依据 SameJob 源码及先前用户运营确认。
- `docs/APPLE_ORGANIZATION_READINESS.md` 分开列明本地内容和生产结果；公网/HTTPS 当前不能判 PASS。

## 三、当前问题与风险

- 新网登录/完整 DNS 导出是网站 DNS 写入的必要外部前提；已向用户请求具体导出操作。
- 浏览器控制报 inventory fetch 失败及 Chrome `cgWindowNotFound`；另一个已有任务正在修复。没有通过其他途径读取登录凭据。
- 最终本地 lint、typecheck、build、release check 和 36/36 浏览器测试全部通过。WebKit 使用 macOS Option-Tab 验证链接键盘导航；预览请求绕过本机 SOCKS 代理。
- 生产验证实际返回 ENOTFOUND，无 HTTP 状态，不能标成 200/404 或上线 PASS。
- 旧 Cloudflare 文档已按用户明确的新长期决定替换；无未解决架构冲突。

## 四、验证状态

| 层级 | 证据与边界 |
| --- | --- |
| 设计 | 用户明确确认当前冻结方案 |
| 代码 | 七个页面及静态配套已实现 |
| 构建 | 当前源码 Astro build、lint、typecheck（0 errors/warnings/hints）与 release check 通过 |
| 自动测试 | Playwright Chromium/WebKit 桌面/移动 36/36 通过（6.8 秒）；含全部站内链接、metadata、404、键盘及溢出检查 |
| 视觉 | 已查看首页桌面/移动全页截图，无明显溢出；非实物手机 |
| GitHub | Public 与 Actions Pages、custom domain 已 API 复核；部署尚未运行 |
| 生产 | `npm run verify:production` 13 个检查均 ENOTFOUND；未通过 |
| 邮件 | 公开 MX 保持不变；未知 selector、完整配置及真实收发未验证 |

## 五、当前基线

- 本地：`/Users/tommacmini/Documents/GitHub/hailinklabs-website`。
- 分支：`codex/company-website`；接手 HEAD：`71d5ce469cae61562ba95bb729ea46aed85c5f80`。
- 本次接手时有同一官网任务的未提交文件；已协调接管，未覆盖无关文件。当前功能结果尚未提交推送。
- 远端：`https://github.com/gaozichen2012/hailinklabs-website.git`，默认主分支 `main`。

## 六、下一步

1. 本地检查已通过；提交官网及文档并推送 main 触发 Pages，核实部署产物。
2. 取得新网完整 DNS 导出，记录并核验备份/回滚，再仅设置四个 A 与 www CNAME。
3. 完成 HTTPS、逐页公网、跳转和邮件保护验证；更新 Apple checklist。邮件实际收发在无工具访问时由用户验收。

## 七、最近重要变化

- 用户冻结 GitHub Pages + 新网 DNS + 腾讯邮箱，取消 Cloudflare 和迁移。
- 完成静态页面并修复 canonical 和键盘焦点；建立 Pages 自动部署流程。
- 仓库审计后改 Public，Pages Actions/custom domain 已配置；DNS 未修改。
