# 项目当前状态

本文件是公开网站工程快照；长期规则见 [项目工程手册](项目工程手册.md)。内部运营资料及完整历史验收记录由私有子模块管理。

## 一、项目总体状态

- 快照日期：2026-09-14。
- 官网使用 Astro 静态构建、GitHub Actions 与 GitHub Pages，正式地址为 `https://hailinklabs.com`。
- 用户已确认公开网站仓库加私有 `internal/` 子模块的资料管理方式。

## 二、当前工作方向

### 网站与自动部署

- 七个业务页面、404、favicon、robots、sitemap、SEO/Open Graph、桌面与移动布局已实现。
- 无运行时后端、表单、数据库、跟踪、外部字体或客户端业务脚本。
- 本次资料迁移不修改网页内容、样式、路由、域名或邮箱配置。

### 公开工程与私有资料

- 主仓库保留网站源码、测试、构建配置、精简公开工程文档及发布审核决定。
- 内部运维、Apple 准备、详细审计与状态记录迁入私有仓库，通过 `internal/` 子模块挂载；主仓库记录引用，不包含子模块文件内容。
- CI 显式关闭子模块拉取；网站工具排除 `internal/`，部署只上传 `dist/`。
- 私有仓库 API 确认 Private；匿名访问仓库 API 与 README 均返回 404，授权访问与拉取正常。
- 无私有子模块的干净源码副本通过 `npm ci`、lint、typecheck（0 errors/warnings/hints）、build、发布检查及 Chromium/WebKit 桌面/移动测试 **44/44**。
- 构建目录全部 14 个文件与迁移前本地构建的 SHA-256 一致，未包含文档或私有文件。此项比对不替代生产环境验收。
- 私有导入历史及主仓库待提交差异的 Gitleaks 扫描未发现 secrets；此结果不等于历史内部资料已撤回。
- 主分支推送后的检查与部署结果以 [GitHub Actions](https://github.com/gaozichen2012/hailinklabs-website/actions/workflows/check.yml) 对应提交记录为准。

## 三、问题与验证边界

- 历史清理独立于子模块迁移：原公开 Git 历史未重写，旧版本内容仍可能公开可读；不声称历史副本已经撤回。
- 私有仓库的地址、子模块路径和引用提交编号属于公开元数据。
- 原网站验收证据归属当时的源码基线，不自动作为本次提交验证结果。移动浏览器测试不等于实物设备测试。

## 四、下一步

按 README 的顺序维护：先推送私有资料，再推送主仓库的子模块引用。旧公开历史如需清理，须另行确认范围并协调已有克隆。

## 五、最近重要变化

- 用户确认公开主仓库与私有子模块的管理方式。
- 内部资料与网站发布依赖分离；公开工程文档精简。
