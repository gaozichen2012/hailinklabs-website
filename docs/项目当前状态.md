# 项目当前状态

本文件是按独立工作方向组织的动态快照；长期规则见 [项目工程手册](项目工程手册.md)。

## 一、项目总体状态

- 快照日期：2026-09-14。
- 当前阶段：**官网已上线，PASS WITH EXTERNAL ACTIONS**。正式地址 `https://hailinklabs.com`，七个业务页面均 HTTPS 200，生产检查 13/13 通过。
- 冻结方案：新网注册/DNS、腾讯企业邮箱、Astro、GitHub Public 仓库与 Pages，新增网站年度成本 0 元。
- 用户已确认企业邮箱收到测试邮件并发送回复；公司收件 PASS。Gmail 端尚未找到回复，外部送达及 SPF/DKIM/DMARC 认证头仍未验证。此项不影响已完成的网站 HTTPS 验收。

## 二、当前工作方向

### 网站与自动部署

- 七个业务页面、404、favicon、robots、sitemap、SEO/Open Graph、桌面和移动布局已实现，无客户端业务脚本、表单、跟踪或后端。
- 已修复 `.html` canonical 与键盘跳过导航焦点；已移除旧 Wrangler/Cloudflare 配置和依赖。
- 仓库已改 Public；操作前 Gitleaks 8.30.1 扫描两个完整历史提交及 39 个候选文件未发现 secrets，见 `REPOSITORY_SECRET_AUDIT.md`。
- Pages 使用 Actions source，绑定 `hailinklabs.com`；底层 hostname 为 `gaozichen2012.github.io`。视觉源码基线 `c1fbcfb907234b3803bb6f6adfe4eb5b81f30083` 的 [Actions 34855700128](https://github.com/gaozichen2012/hailinklabs-website/actions/runs/34855700128) check/deploy 均 success；之前两轮为 [34854518100](https://github.com/gaozichen2012/hailinklabs-website/actions/runs/34854518100)（重跑成功）、[34855292340](https://github.com/gaozichen2012/hailinklabs-website/actions/runs/34855292340)。

### 官网视觉与交互

- 结果：**PASS**。完成初始生产审计及三轮“修改 → 部署 → 正式浏览器复查”；采用白色/浅灰、系统字体、克制绿色强调、统一间距与产品优先的排版。首页和 SameJob 以文字形成视觉中心，没有虚构截图或引入新资源。
- 全站导航保持直接可见，点击区域至少 44×44px；链接反馈为 160ms 的颜色/2px 箭头变化，支持 `prefers-reduced-motion`。Privacy 标题和正文统一为 680px 阅读列，正文桌面 17px / 移动 16px；隐私文件及已确认事实未改动。
- 源码 `c1fbcfb` 本地 lint、typecheck（0 errors/warnings/hints）、release check、build 全通过。原有 36 项测试保留，新增全站 320/768px、标题层级、触控尺寸、键盘焦点与减少动画检查；本地及正式域名 Chromium/WebKit 桌面/移动均 **44/44 PASS**。
- 真实 Production 浏览器逐页审查全部七页 1440×900 与 390×844，另复核 320px 首页/Privacy、768px SameJob；最终无明显裁切、溢出、孤词换行或导航问题。实际点击首页 → Products → SameJob → Privacy 路径成功。
- 生产 HTTPS/跳转/资源 **13/13 PASS**；七页 HTML、favicon、robots、sitemap、CSS 共 11 个文件 SHA-256 与当前构建一致。CSS 10,933 bytes，无客户端脚本、外部字体、第三方请求或新增依赖。
- 首轮 Actions 曾在 FinalizeArtifact 返回 403，原作业重跑后部署成功，未修改权限或发布流程；后两轮均直接成功。截图与详细证据在 `artifacts/design-before/`、`artifacts/design-round-1/`、`artifacts/design-round-2/`、`artifacts/design-after/`，均不入 Git。
- 本视觉方向未修改 DNS、邮箱 DNS、GitHub Pages 域名或 Apple 配置；并行邮件方向的 SPF 变更及证据保持独立，不作为本方向操作或验证结果。

### DNS、HTTPS 与邮箱保护

- 完整写前导出保存于 `artifacts/dns-backups/hailinklabs-before-20260914.csv`，仅两条 MX 和两条 NS。写后完整页面七条记录及公开权威答案均已核对；详细哈希、分类、字段与回滚见 `DOMAIN_AND_DNS.md`。
- 新网限制同主机同类型最多两条记录。用户明确批准两条 A 继续上线：`185.199.108.153`、`185.199.109.153`；www CNAME 为 `gaozichen2012.github.io`，均默认线路、TTL 600 秒。.110/.111 未保存，不再尝试增加。
- NS 保持 `ns11.xincache.com`、`ns12.xincache.com`；MX 保持 priority 5 `mxbiz1.qq.com`、priority 10 `mxbiz2.qq.com`，TTL 600。原记录所有已展示字段及时间戳未变。**Email DNS modified: NO**。
- 原完整 zone 不含 SPF、DKIM、DMARC 或邮箱验证 TXT；此次保持缺省状态，未添加邮箱认证配置。缺省不等于邮件认证 PASS。
- GitHub apex/www 证书已批准，到期日 2026-12-13；Enforce HTTPS 已启用。正常证书校验下，HTTP→HTTPS、www→apex 均 301，保留路径及参数。
- 已发送主题为 `Hailink Labs email verification 2026-09-14` 的外部测试邮件到公司邮箱。用户确认收到并已回复；公司收件 PASS，发件动作由用户确认。Gmail 全邮箱搜索及原会话尚无回复，外部送达与认证头 NOT VERIFIED。

### Apple 官网准备

- 正式页面的公司品牌、法律主体、品牌关系、SameJob 产品内容、公司域名邮箱、Contact、Privacy、Support、无占位内容等网站检查全部 PASS。
- 已按 Apple 官方组织入驻要求重新核验并更新 `APPLE_ORGANIZATION_READINESS.md`。不将网站通过等同法律主体、D-U-N-S、签署权限、2FA 或入驻批准。

## 三、当前问题与边界

- 企业邮箱收件已由用户确认；待核对回复的收件地址与是否有退信。企业微信网页受浏览器站点安全策略限制，未绕过；无需用户再操作 DNS 或 GitHub。
- 用户已手动完成域名所有权变更并取得新网管理权限。截图显示当前注册人为个人，不能声称公司持有；注册日 2026-04-04，到期日 2027-04-04。不擅自再次过户。
- 浏览器已恢复。项目本机 `.codex/config.toml` 代理覆盖由 `.git/info/exclude` 排除发布，保留原运行时/权限；凭据与 DNS 原始备份不入 Git。
- 移动验收为浏览器视口与 Chromium/WebKit 测试，不声称实物 iPhone 验证；Apple 服务与 App 上架不在此次网站验收范围。

## 四、验证状态

| 层级 | 证据与边界 |
| --- | --- |
| 设计 | 三轮生产视觉迭代完成，极简、系统字体、产品聚焦；七页桌面/移动逐页确认 |
| 代码与构建 | 视觉源码 `c1fbcfb` 的 lint、typecheck、release check、Astro build 全通过 |
| 自动测试 | 本地与正式域名 Chromium/WebKit 桌面/移动均 44/44；原 36 项未削弱，新增全站 320/768px、触控与减少动画检查 |
| GitHub | Public、Actions source、custom domain、证书和 HTTPS enforcement 已复核；已部署 |
| 生产 HTTP/HTTPS | `npm run verify:production` 13/13 PASS：七页及三项静态资源 200、自定义 404、两项跳转；正常证书验证，未跳过 TLS 校验 |
| 生产移动 | 七页真实浏览器 390×844 通过；320px 与 768px 全站自动检查通过，无水平溢出 |
| 视觉 | 七页 1440×900 / 390×844 最终截图留档；首页、SameJob、Privacy、About、Contact、Support、Header/Footer 已复核 |
| 站外链接 | Apple/GitHub 隐私政策链接 200；Apple 入驻依据已重新读取官方页面 |
| 邮件 | 完整原记录与两台权威 NS 核验 PASS；公司收件 PASS（用户确认），用户确认已发回复；外部送达及邮件头 NOT VERIFIED |

## 五、当前基线

- 本地：`/Users/tommacmini/Documents/GitHub/hailinklabs-website`，分支 `codex/visual-refinement`；远端默认分支 `main`。
- 视觉源码提交 `c1fbcfb`；后续状态文档提交不改变上述源码及生产产物。最终文档提交与推送状态由 Git 历史记录。
- 证据（不入 Git）：`artifacts/production-verification.json`、`artifacts/production-mobile.json`、`artifacts/pages-final.json`、`artifacts/dns-after.json`、`artifacts/email-verification.json`、`artifacts/FINAL_ACCEPTANCE.md`。

## 六、下一步

核对已发回复的收件地址与退信情况；Gmail 收到后读取认证头，补齐邮件验收项。网站、DNS、HTTPS 已完成，无需新增服务或继续改配置。

官网视觉方向已完成并部署，无剩余视觉修复项；生产验收报告见 `artifacts/design-after/acceptance.md`。

## 七、最近重要变化

- 冻结 GitHub Pages + 新网 DNS + 腾讯邮箱，取消 Cloudflare 和迁移，维持零新增网站成本。
- 官网完成三轮生产视觉优化：系统字体、首页产品聚焦、SameJob 层级、Privacy 阅读列、移动换行及可访问交互。
- 用户取得域名管理权限，完整 zone 备份后批准两条 A 例外；三条网站记录已生效，邮箱与 NS 未变。
- 正式站与本地 44/44 测试通过；HTTPS/资源/跳转 13/13，11 项生产文件哈希与构建一致。
- 用户确认公司邮箱收到测试邮件并已发回复；外部送达与认证头仍待验证。
