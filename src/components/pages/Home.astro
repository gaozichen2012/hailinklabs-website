---
import Layout from '../../layouts/Layout.astro';
import Product from '../../components/Product.astro';
import { site } from '../../data/site';

import {
  translator,
  localizedPath,
  companyName,
  type Locale,
} from '../../data/i18n';
interface Props {
  locale: Locale;
}
const { locale } = Astro.props;
const t = translator(locale);
const href = (path: string) => localizedPath(path, locale);
---

<Layout
  locale={locale}
  title={t('Hailink Labs — Focused software for everyday work')}
  description={t(
    'Hailink Labs is a software company based in Shenzhen, China. We build focused apps for everyday work, including Invoice Maker: SameJob.',
  )}
>
  <section class="hero">
    <p class="eyebrow">{t('Hailink Labs · Independent software')}</p>
    <h1>
      {t('Focused software.')}
      <br />
      <span class="muted-heading">{t('For everyday work.')}</span>
    </h1>
    <div class="hero-bottom">
      <p>
        {t('We build practical apps with a simple purpose:')}
        <br class="desktop-break" />{' '}
        {t('make the work you do every day a little easier.')}
      </p>
      <a class="button" href={href('/products')}>
        {t('Meet our products')} <span aria-hidden="true">→</span>
      </a>
    </div>
  </section>
  <section class="products-section" aria-labelledby="products-title">
    <div class="section-label">
      <h2 id="products-title">{t('Our products')}</h2>
    </div>
    <Product locale={locale} />
  </section>
  <section class="section-row company-section" aria-labelledby="company-title">
    <div class="section-label">
      <h2 id="company-title">{t('The company')}</h2>
    </div>
    <div class="section-body">
      <p class="large-copy">
        {t('Built in Shenzhen.')}
        <br />
        {t('Designed for a')}{' '}
        <span class="keep-together">{t('global audience.')}</span>
      </p>
      <p>
        {t(
          'Hailink Labs is a software company based in Shenzhen, China. We focus on simplicity, reliability, and a practical user experience.',
        )}
      </p>
      <p class="legal-note">
        {t('Hailink Labs is the brand of')} <span>{companyName(locale)}</span>
        {locale === 'zh-CN' ? '。' : ''}
      </p>
      <a class="text-link" href={href('/about')}>
        {t('About Hailink Labs')} <span aria-hidden="true">→</span>
      </a>
    </div>
  </section>
  <section class="section-row contact-section" aria-labelledby="contact-title">
    <div class="section-label">
      <h2 id="contact-title">{t('Get in touch')}</h2>
    </div>
    <div class="section-body">
      <p class="large-copy">
        {t('A question or an idea?')}
        <br />
        {t('We’d like to hear it.')}
      </p>
      <a class="email-link" href={`mailto:${site.email}`}>
        {site.email} <span aria-hidden="true">→</span>
      </a>
    </div>
  </section>
</Layout>
