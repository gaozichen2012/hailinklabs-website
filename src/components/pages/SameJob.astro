---
import Page from './ProductDetail.astro';
import { products } from '../../data/products';
import type { Locale } from '../../data/i18n';
interface Props {
  locale: Locale;
}
const { locale } = Astro.props;
---

<Page locale={locale} product={products[0]} />
